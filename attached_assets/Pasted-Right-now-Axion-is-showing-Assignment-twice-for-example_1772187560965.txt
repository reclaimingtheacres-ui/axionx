Right now Axion is showing “Assignment” twice (for example “Grant-Admin” in one place and “Unassigned” in another), which means you’ve got two different fields driving the UI. That needs to become a single source of truth so the job’s assignment, the next booking assignment, and the schedule list all reference the same “assigned_to” value unless the user explicitly changes it.

The clean fix is to make “Assigned to” a single field on the Job (for example job.assigned_to_user_id) and have everything else inherit from it by default. The “Update Status / Visit Type / Assignment” panel should update the Job record only, and the Schedule booking form should default its “Assigned to” to the Job’s assigned user. If the Job is unassigned, the schedule booking should also show unassigned and save unassigned unless changed. This prevents the scenario where the header shows “Grant-Admin” while the schedule shows “Unassigned”.

In practical terms, remove any separate “booking assigned_to” display from the header unless you are genuinely allowing a booking to be assigned to a different person than the job. If you do want that flexibility later, then the UI must be explicit: show “Job assigned to” and “Booking assigned to” as two clearly labelled values, and the booking should default to job assignment but allow override. If you do not need that complexity now, keep it simple and enforce that booking assignment always equals job assignment, meaning you can delete schedule.assigned_to_user_id entirely and compute it from job.assigned_to_user_id on display.

For the schedule inputs, keep the dual-entry approach you asked for, but only store one actual datetime. The picker can remain for convenience, but the values you submit should be the manual dd/mm/yyyy and hh:mm am/pm fields, then you parse and store to scheduled_for. The Booking Type dropdown should be the single field used for “New Visit / Re-attend / Urgent New Visit / Update Required / Urgent Update Required”, and the optional notes should remain as free text.

If you want the panel to read exactly as you wrote, the intended default state should be: Status = New, Visit Type = New Visit, Assignment = Unassigned, then Schedule fields below with date and time empty placeholders and Booking type/notes. When the user changes Assignment in that top panel, it should immediately update the job assignment and also update the schedule form’s assignment (or the hidden schedule assignment value) so both remain identical.

If you paste the names of the two fields currently being displayed (for example job.assigned_to versus schedule.assigned_to or similar), I’ll tell you which one to delete/stop rendering so “Unassigned” and “Grant-Admin” can never conflict again.