Add these imports (top of app.py)
from werkzeug.security import generate_password_hash, check_password_hash

(If you don’t already have session imported, add from flask import session too.)

2) Add two helper functions

Put near your other helpers:

def users_count():
    conn = db()
    cur = conn.cursor()
    cur.execute("SELECT COUNT(*) c FROM users")
    c = cur.fetchone()["c"]
    conn.close()
    return c
3) Replace your current login check with hashed passwords

Replace this line in /login POST:

if not user or user["password"] != password:

with:

if not user or not check_password_hash(user["password"], password):
4) Add /signup routes (first user becomes admin)

Add these routes:

@app.get("/signup")
def signup():
    # Allow signup only if no users exist yet (bootstrap first admin)
    if users_count() > 0:
        return ("Signup disabled. Ask admin to create your account.", 403)

    return """
    <h3>Create Admin Account</h3>
    <form method='post'>
        <input name='full_name' placeholder='Full name' required><br>
        <input name='email' placeholder='Email' required><br>
        <input name='password' type='password' placeholder='Password' required><br>
        <button>Create Admin</button>
    </form>
    """
@app.post("/signup")
def signup_post():
    if users_count() > 0:
        return ("Signup disabled. Ask admin to create your account.", 403)

    full_name = request.form.get("full_name", "").strip()
    email = request.form.get("email", "").strip().lower()
    password = request.form.get("password", "")

    if not full_name or not email or not password:
        return ("Missing fields", 400)

    hashed = generate_password_hash(password)

    conn = db()
    cur = conn.cursor()
    cur.execute("""
        INSERT INTO users (full_name, email, password, role, active, created_at)
        VALUES (?, ?, ?, 'admin', 1, ?)
    """, (full_name, email, hashed, now_ts()))
    conn.commit()
    conn.close()

    # Auto login
    session["user_id"] = 1
    session["role"] = "admin"

    return redirect(url_for("admin_dashboard"))
5) Add /logout
@app.get("/logout")
def logout():
    session.clear()
    return redirect(url_for("login"))
6) Update your /login page to include a signup link for first-time setup

Change the login HTML to:

@app.get("/login")
def login():
    signup_link = ""
    if users_count() == 0:
        signup_link = "<p><a href='/signup'>Create Admin Account</a></p>"

    return f"""
    <h3>Login</h3>
    <form method='post'>
        <input name='email' placeholder='Email' required><br>
        <input name='password' type='password' placeholder='Password' required><br>
        <button>Login</button>
    </form>
    {signup_link}
    """
7) Add admin-only “Create User” page (for agents)

Add:

@app.get("/admin/users/new")
@admin_required
def admin_user_new():
    return """
    <h3>Create User</h3>
    <form method='post'>
        <input name='full_name' placeholder='Full name' required><br>
        <input name='email' placeholder='Email' required><br>
        <input name='password' type='password' placeholder='Temp password' required><br>
        <select name='role'>
            <option value='agent'>Agent</option>
            <option value='admin'>Admin</option>
        </select><br>
        <button>Create</button>
    </form>
    """
@app.post("/admin/users/new")
@admin_required
def admin_user_create():
    full_name = request.form.get("full_name", "").strip()
    email = request.form.get("email", "").strip().lower()
    password = request.form.get("password", "")
    role = request.form.get("role", "agent")

    if not full_name or not email or not password:
        return ("Missing fields", 400)

    hashed = generate_password_hash(password)

    conn = db()
    cur = conn.cursor()
    cur.execute("""
        INSERT INTO users (full_name, email, password, role, active, created_at)
        VALUES (?, ?, ?, ?, 1, ?)
    """, (full_name, email, hashed, role, now_ts()))
    user_id = cur.lastrowid
    conn.commit()
    conn.close()

    audit("user", user_id, "create", f"User created: {full_name} ({role})", {"email": email, "role": role})
    return redirect(url_for("admin_dashboard"))